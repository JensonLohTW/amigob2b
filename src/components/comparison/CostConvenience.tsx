/**
 * 成本便利性对比组件
 * 重构后的简化版本，使用模块化架构
 */

'use client'

import { CostConvenience as CostConvenienceFeature } from '@/features/cost-convenience'

export function CostConvenience() {
  return <CostConvenienceFeature />
}
